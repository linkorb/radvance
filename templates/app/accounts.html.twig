{% extends "base.html.twig" %}

{% block content %}
    <h1>My accounts</h1>
    <ul>
        {% for account in accounts %}
        <li><a href="{{ path('space_index', { 'accountName': account.getName() } ) }}">{{ account.getName() }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
