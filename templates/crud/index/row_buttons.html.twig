<a href="{{ path(name ~ '_view', { 'id': entity.getId() }) }}" class="btn btn-primary btn-sm" title="View">
    <i class="fa fa-eye"></i>
</a>

<a href="{{ path(name ~ '_edit', { 'id': entity.getId() }) }}" class="btn btn-success btn-sm" title="Edit">
    <i class="fa fa-edit"></i>
</a>

<a href="{{ path(name ~ '_delete', { 'id': entity.getId() }) }}" class="btn btn-danger btn-sm" title="Delete">
    <i class="fa fa-trash"></i>
</a>
